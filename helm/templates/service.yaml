apiVersion: v1
kind: Service
metadata:
  name: {{ include "ansm-atc-search.fullname" . }}
  labels:
    {{- include "ansm-atc-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ansm-atc-search.selectorLabels" . | nindent 4 }}
